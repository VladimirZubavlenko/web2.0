enum Routes {
  ROOT = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  COUNTER = '/counter',
  USERS = '/users',
  CHALLENGES = '/challenges',
  ERROR = '/error',
  NEWS = '/news'
}

export enum RoutesNames {
  ROOT = 'ROOT',
  LOGIN = 'Login',
  REGISTER = 'Register',
  COUNTER = 'COUNTER',
  USERS = 'USERS',
  CHALLENGES = 'CHALLENGES',
  ERROR = 'ERROR',
  NEWS = 'NEWS'
}

export enum NavBarNames {
  LOGIN = 'Login',
  REGISTER = 'Register',
}

export default Routes;
